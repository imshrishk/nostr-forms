export const decodeForm = (event) => {
    // Placeholder implementation
    return {
      name: event.name || 'Untitled Form',
      description: event.description || '',
      fields: []
    };
  };